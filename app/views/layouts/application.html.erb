<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= content_for?(:title) ? yield(:title) : "Comiccon" %></title>

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "vendor/modernizr" %>
  <%= csrf_meta_tags %>
</head>

<body>
  <div class="container">
    <%= render 'layouts/header' %>
    <div class="row">
      <% flash.each do |key, value| %>
        <div class="alert-box <%= key %> radius"><%= value %></div>
      <% end %>
    </div>
    <div>
        <%= yield %>
    </div>
  </div>
<%= javascript_include_tag "application" %>
</body>
</html>
